

// "let" is keyword used to define variables

let num = 4 //  num is like box where value will be stored so that we can reuse anywhere

console.log(num)

num = 9 // we don't use let again because we already defined "let" for "num"
console.log(num)

let username = "chandu"

console.log(username)


/*
RULES
Variable name can have characters
variable can have numbers except first position
can have special symbols $ and _

user_name ==> snake casing rule
userName ==> camel casing rule

 */